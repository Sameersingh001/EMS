<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= emp.name %> Profile</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-pink-300 via-purple-300 to-indigo-400 font-sans min-h-screen flex items-center justify-center p-4">

  <div class="w-full max-w-4xl bg-white rounded-3xl shadow-xl overflow-hidden ring-2 ring-purple-300 ring-opacity-50 transition duration-300 hover:shadow-2xl">
    
    <!-- Header -->
    <div class="flex flex-col sm:flex-row items-center sm:items-start sm:space-x-6 p-8 bg-gradient-to-r from-indigo-600 to-purple-600 text-white">
        
      <div class="w-32 h-32 rounded-full bg-white overflow-hidden border-4 border-white shadow-lg transform transition duration-300 hover:scale-105">
        <img src="<%= emp.imageUrl %>" alt="Profile Image" class="w-full h-full object-cover">
      </div>
      <div class="mt-4 sm:mt-0 text-center sm:text-left">
        <h1 class="text-3xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-pink-300 to-white"><%= emp.name %></h1>
        <p class="text-gray-200 text-lg">@<%= emp.username %></p>
        <span class="inline-block mt-3 px-4 py-1 text-sm font-bold rounded-full shadow-md 
          <%= emp.status === 'Active' ? 'bg-green-500 text-white' : 'bg-red-500 text-white' %>">
          <%= emp.status %>
        </span>
      </div>
    </div>




    <!-- Body -->
    <div class="p-8 space-y-8">

      <div class="flex flex-row items-center justify-around">
      
      <a href="/delete/<%=emp.id %>"><button class=" w-32 bg-red-500 transition transform hover:scale-105 hover:bg-red-800 text-2xl h-10  text-white font-bold rounded-2xl shadow-2xl ">Delete</button></a>

       <a href="/admin"><button class=" w-64 bg-blue-500 transition transform hover:scale-105 hover:bg-blue-800 text-2xl h-10  text-white font-bold rounded-2xl shadow-2xl ">Back to Admin</button></a>

       <a href="/admin/update/emp/<%= emp.id %>"><button class=" w-32 bg-green-500 transition transform hover:scale-105 hover:bg-green-800 text-2xl h-10 text-white font-bold rounded-2xl  shadow-2xl ">Update</button></a>

    </div>

      <!-- Grid Info -->
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
        <div class="bg-indigo-50 p-4 rounded-xl shadow-md">
          <h3 class="font-bold text-indigo-700 mb-1">Email</h3>
          <p class="text-gray-800 break-words"><%= emp.email %></p>
        </div>
        <div class="bg-blue-50 p-4 rounded-xl shadow-md">
          <h3 class="font-bold text-blue-700 mb-1">Phone</h3>
          <p class="text-gray-800"><%= emp.phone %></p>
        </div>
        <div class="bg-purple-50 p-4 rounded-xl shadow-md">
          <h3 class="font-bold text-purple-700 mb-1">Department</h3>
          <p class="text-gray-800"><%= emp.department %></p>
        </div>
        <div class="bg-pink-50 p-4 rounded-xl shadow-md">
          <h3 class="font-bold text-pink-700 mb-1">Position</h3>
          <p class="text-gray-800"><%= emp.position %></p>
        </div>
        <div class="bg-green-50 p-4 rounded-xl shadow-md">
          <h3 class="font-bold text-green-700 mb-1">Salary</h3>
          <p class="text-gray-800 font-medium">₹ <%= emp.salary.toLocaleString() %></p>
        </div>
        <div class="bg-yellow-50 p-4 rounded-xl shadow-md">
          <h3 class="font-bold text-yellow-700 mb-1">Date of Joining</h3>
          <p class="text-gray-800"><%= emp.dateOfJoining.toDateString() %></p>
        </div>
      </div>

      <!-- About -->
      <div class="bg-violet-50 p-4 rounded-xl shadow-md">
        <h3 class="font-bold text-violet-700 mb-2 text-lg">About</h3>
        <p class="text-gray-800 leading-relaxed"><%= emp.about %></p>
      </div>

      <!-- Address -->
      <div class="bg-fuchsia-50 p-4 rounded-xl shadow-md">
        <h3 class="font-bold text-fuchsia-700 mb-2 text-lg">Address</h3>
        <p class="text-gray-800 leading-relaxed"><%= emp.address %></p>
      </div>
     
    </div>
  </div>
</body>
</html>
